startCommand:
  type: http
  configSchema:
    type: object
    properties:
      apiKey:
        type: string
        title: API Key
        description: "Get your free API key at https://api.meacheal.ai/apply"
    required: []
  url: https://api.meacheal.ai/mcp
  headers:
    Authorization: "Bearer ${apiKey}"
